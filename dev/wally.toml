[package]
name = "vyon/linktracker-dev"
description = "LinkTracker is a package that allows developers to do more with promotional content/material."
version = "1.0.0"
registry = "https://github.com/UpliftGames/wally-index"
license = "MIT"
realm = "shared"
authors = ["Vyon (https://github.com/Vyon)"]

exclude = ["**"]
include = ["src", "src/*", "default.project.json", "wally.lock", "wally.toml"]

[dependencies]
# Interface
Fusion = "elttob/fusion@0.2.0"
